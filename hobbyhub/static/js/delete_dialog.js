function openDeleteDialog(dialog_id) {
     dialog = document.getElementById(dialog_id)
     dialog.showModal();
}

function closeDeleteDialog(dialog_id) {
     dialog = document.getElementById(dialog_id)
     dialog.close();}
